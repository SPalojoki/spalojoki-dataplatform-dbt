spalojoki_dataplatform:
    target: prod
    outputs:
        prod:
            type: bigquery
            method: oauth-secrets
            project: "{{ env_var('GCP_PROJECT_ID') }}"
            dataset: analytics
            threads: 4
            token: "{{ env_var('GCP_TOKEN') }}"